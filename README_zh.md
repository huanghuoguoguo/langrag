<div align="center">
  <a href="README.md">English</a> | <a href="README_zh.md">中文</a>
</div>

<p align="center">
  <img src="docs/logo.svg" alt="LangRAG Logo" width="300"/>
</p>

<h1 align="center">LangRAG</h1>

<p align="center">
  <strong>用于构建智能知识系统的模块化、生产级 RAG 内核</strong>
</p>

<p align="center">
  <a href="#功能特性">功能特性</a> •
  <a href="#架构设计">架构设计</a> •
  <a href="#快速开始">快速开始</a> •
  <a href="#文档">文档</a> •
  <a href="#路线图">路线图</a> •
  <a href="#框架对比">框架对比</a>
</p>

---

## 什么是 LangRAG？

**LangRAG** 是一个 **"小而美" 的 RAG 内核**。它旨在成为您的智能知识系统核心中轻量级且强大的引擎。

LangRAG 实现了独特的平衡：它既贯彻了 **行业标准的最佳实践**（如父子索引、混合检索和 LLM 裁判），又保持了 **极简的体积** 和 **扁平透明的代码结构**。

我们要践行的是 **开箱即用** 的理念，但不牺牲可控性。您将获得生产级的原语——内置了遥测、缓存和评估功能——而无需承受单体框架的沉重负担和“黑魔法”。

> 🎯 **为什么选择 LangRAG?**
> *   **观点鲜明且就绪**：RRF 和递归分块等最佳实践是默认配置，拒绝配置地狱。
> *   **透明内核**：核心代码仅约 3k 行，没有深层抽象。您可以在几分钟内阅读、理解并修改代码。
> *   **应用驱动**：LangRAG 是为您所用的库，而不是把您框住的框架。

`web/` 目录包含一个 **演示应用程序**，展示了如何用极少的胶水代码构建一个复杂的、“工业级”的 RAG 流程。

---

## 功能特性

### ✅ 已实现 (v0.2)

| 类别 | 功能 | 描述 |
|----------|---------|-------------|
| **索引** | 多格式解析 | PDF, DOCX, Markdown, HTML, TXT |
| | 智能分块 | 带重叠的递归字符分割器 |
| | 父子索引 | 针对长文档的层级检索 |
| | QA 索引 | 提取问答对以实现精确匹配 |
| | 批处理 | 带进度跟踪的大规模文档索引 |
| **存储** | 向量库 | DuckDB (持久化, 混合检索), ChromaDB, SeekDB (混合检索) |
| | KV 存储 | 基于 SQLite 的持久化键值存储 |
| | 网络搜索 | 实时网络集成 (Bing, Google, DuckDuckGo) |
| **检索** | 混合检索 | 向量 + BM25 全文检索及 RRF 融合 (DuckDB, SeekDB) |
| | 智能路由 | 基于 LLM 的知识库选择 |
| | 查询重写 | 语义查询优化 |
| | 重排序 | 集成 Cohere, Qwen, NoOp 提供商 |
| | 语义缓存 | 基于相似度的查询缓存，支持 TTL 和 LRU 淘汰 |
| **评估** | LLM 裁判 | 忠实度 (Faithfulness), 回答相关性 (Answer Relevancy), 上下文相关性 (Context Relevancy) 指标 |
| | 批量评估 | 带进度回调的多样本评估 |
| | 评估报告 | 聚合统计和单样本结果 |
| **可观测性** | OpenTelemetry | 检索和索引管道的分布式链路追踪 |
| **生成** | 流式传输 | 用于实时响应的 Server-Sent Events (SSE) |
| | LLM 抽象 | 支持注入的 OpenAI 兼容接口 |
| | 多阶段 LLM | 基于阶段的模型配置 (chat, router, rewriter, reranker) |
| **测试** | 全套测试 | 单元测试, 集成测试 (500+ 测试用例) |

### 🔧 架构亮点

- **依赖注入**：LLM、Embedder 和 VectorStore 是注入的，而非内部管理。
- **多阶段 LLM**：为不同任务（对话、路由、重写、重排序）配置不同的模型。
- **工厂模式**：轻松注册和创建自定义组件。
- **异步优先**：核心 API 支持 async/await 以实现高并发。
- **类型安全**：所有配置和实体均使用 Pydantic 模型。
- **可观测**：内置 OpenTelemetry 追踪支持。

---

## 架构设计

```
src/langrag/
├── config/            # 配置管理 (Pydantic)
├── core/              # 回调和事件系统
├── datasource/        # 存储抽象
│   ├── kv/            # 键值存储 (InMemory, SQLite)
│   └── vdb/           # 向量数据库 (Chroma, DuckDB, SeekDB, Web)
├── entities/          # 领域模型 (Document, Dataset, SearchResult)
├── index_processor/   # 索引管道
│   ├── extractor/     # 文档解析器 (PDF, DOCX, MD, HTML, TXT)
│   ├── splitter/      # 文本分块器 (Recursive, FixedSize)
│   ├── processor/     # 索引策略 (Paragraph, ParentChild, QA)
│   └── cleaner/       # 文本清洗
├── llm/               # LLM 抽象
│   ├── embedder/      # Embedding 提供商
│   ├── providers/     # LLM 提供商 (OpenAI 兼容, 本地)
│   └── stages.py      # 多阶段 LLM 配置 (chat, router, rewriter, reranker)
├── retrieval/         # 检索管道
│   ├── router/        # 知识库路由
│   ├── rewriter/      # 查询重写
│   ├── rerank/        # 结果重排序
│   ├── compressor/    # 上下文压缩
│   └── workflow.py    # 编排流程
├── cache/             # 语义缓存层
├── batch/             # 大规模索引的批处理
├── evaluation/        # LLM 裁判评估框架
│   └── metrics/       # 忠实度, 回答/上下文相关性
├── observability/     # OpenTelemetry 追踪集成
└── utils/             # 工具类 (RRF, 相似度, 异步助手)
```

---

## 快速开始

### 安装

```bash
git clone https://github.com/huanghuoguoguo/langrag.git
cd langrag

# 使用 uv 安装 (推荐)
uv sync --dev
```

### 选项 1: 运行 Web 演示

```bash
./web/start.sh
# 或者: uv run python -m web.app
```

访问: [http://localhost:8000](http://localhost:8000)

### 选项 2: 作为库使用

```python
from langrag import (
    Dataset,
    SimpleTextParser,
    RecursiveCharacterChunker,
    ParentChildIndexProcessor
)
from langrag.datasource.vdb.duckdb import DuckDBVector
from langrag.datasource.kv.sqlite import SQLiteKV

# 1. 解析文档
parser = SimpleTextParser()
docs = parser.parse("knowledge_base.txt")

# 2. 创建数据集和存储
dataset = Dataset(name="my_kb", collection_name="my_collection")
vector_store = DuckDBVector(dataset, database_path="./vectors.duckdb")
kv_store = SQLiteKV(db_path="./parents.sqlite")

# 3. 使用父子策略进行索引
processor = ParentChildIndexProcessor(
    vector_store=vector_store,
    kv_store=kv_store,
    embedder=my_embedder,  # 注入你的 embedder
    parent_splitter=...,
    child_splitter=...
)
processor.process(dataset, docs)

# 4. 搜索
results = vector_store.search("your query", query_vector=[...], top_k=5)
```

---

## 文档

LangRAG 使用带有 Material 主题的 MkDocs 提供全面的文档。

### 查看文档

```bash
# 安装文档依赖
uv sync --extra docs

# 本地服务文档
uv run mkdocs serve

# 构建静态文档
uv run mkdocs build
```

访问: [http://localhost:8000](http://localhost:8000) 查看本地文档。

### 文档结构

- **Getting Started**: 安装，快速开始
- **User Guide**: 核心概念，文档处理，检索工作流，评估
- **API Reference**: 所有模块的完整 API 文档

---

## 路线图

### ✅ v0.2 (已完成)

- [x] **DuckDB FTS**: 基于 BM25 和 RRF 混合融合的全文检索
- [x] **语义缓存**: 基于相似度的缓存，支持 TTL 和 LRU 淘汰
- [x] **批处理**: 带进度跟踪的大规模文档索引
- [x] **LLM 裁判**: 评估框架 (忠实度, 回答/上下文相关性)
- [x] **OpenTelemetry**: 分布式追踪集成
- [x] **API 文档**: 基于 MkDocs 的综合文档

### 🚀 v0.3 (进行中)
- [x] **Agents**: 工具使用 (Tool-use) 和多步推理框架
- [x] **RAPTOR**: 用于树状组织检索的递归摘要处理 (Recursive Abstractive Processing for Tree-Organized Retrieval)
- [ ] **Graph RAG**: 知识图谱集成
- [ ] **自适应检索**: 基于查询类型的动态策略选择
- [ ] **评估基准**: 内置评估数据集 (BEIR, MTEB)

### 未来规划
- [ ] **多模态**: 图像和音频文档支持
- [ ] **云连接器**: 用于文档摄入的 S3, GCS, Azure Blob 支持

---

## 与其他 RAG 框架的对比

| 特性 | LangRAG | LangChain | LlamaIndex | PowerRAG |
|---------|---------|-----------|------------|----------|
| **定位** | RAG 内核 | 通用 LLM 框架 | 数据框架 | 生产级平台 |
| **哲学** | 注入而非管理 | All-in-one | 以索引为中心 | 面向服务 (DB为中心) |
| **存储** | 灵活 (DuckDB/SeekDB) | 无关性 (Agnostic) | 无关性 (Agnostic) | OceanBase (SQL+Vector) |
| **智能路由** | ✅ LLM 驱动 | ✅ Chains | ✅ Router | ✅ 对话式 (Conversational) |
| **父子索引** | ✅ 内置 | ✅ 支持 | ✅ 支持 | ✅ 支持 |
| **RAA/RAPTOR** | ✅ 内置 | ⚠️ 手动 | ✅ 支持 | ⚠️ 手动 |
| **混合检索** | ✅ DuckDB, SeekDB | ✅ Ensemble | ✅ 外部 | ✅ OceanBase |
| **语义缓存** | ✅ 内置 | ❌ 外部 | ❌ 外部 | ❌ 外部 |
| **LLM 裁判评估** | ✅ 内置 | ⚠️ 集成 | ✅ 内置 | ✅ 集成 (Langfuse) |
| **OpenTelemetry** | ✅ 原生 | ⚠️ 部分 | ⚠️ 部分 | ⚠️ 集成 |
| **网络搜索集成** | ✅ 多提供商 | ✅ Tools | ✅ Tools | ✅ Tools |
| **轻量级** | ✅ ~3k LOC 核心 | ❌ 庞大 | ❌ 庞大 | ❌ 重型 (Docker Compose) |
| **类型安全** | ✅ Pydantic | ⚠️ 部分 | ✅ Pydantic | ✅ Pydantic |

### 为什么选择 LangRAG?

1.  **是内核，不是框架**：LangRAG 为您提供 RAG 原语，而不强加应用程序结构。
2.  **注入优先**：您的应用程序拥有 LLM、Embedder 和存储的所有权。LangRAG 只是负责编排。
3.  **高级索引**：开箱即用的内置父子索引和 QA 索引策略。
4.  **内置评估**：用于检索质量评估的 LLM 裁判框架。
5.  **生产就绪**：语义缓存、批处理和 OpenTelemetry 追踪。
6.  **全面测试**：拥有 500+ 测试用例，覆盖全面的边缘情况。
7.  **极简依赖**：核心库具有极少的外部依赖。

---

## 开发

```bash
# 运行核心测试 (LangRAG 库)
uv run pytest tests/

# 运行 web 演示测试
uv run pytest web/tests/ -m "not local_llm"

# 运行集成测试
uv run pytest tests/integration/

# 带覆盖率运行
./run_tests.sh

# 代码检查和格式化
uv run ruff check src/ tests/
uv run ruff format src/ tests/
```

### 测试结构

```
tests/              # LangRAG 核心测试
├── unit/           # 单元测试 (464 tests)
└── integration/    # 集成测试 (DuckDB, SeekDB 验证)

web/tests/          # Web 演示测试
├── unit/           # Web 组件的单元测试
└── test_api.py     # API 集成测试
```

### 可选依赖

```bash
# 文档解析器 (PDF, DOCX 等)
pip install langrag[parsers]

# Reranker 支持
pip install langrag[reranker]

# OpenTelemetry 可观测性
pip install langrag[observability]

# 文档生成
pip install langrag[docs]

# 所有功能
pip install langrag[all]
```

---

## License

MIT License

---

<p align="center">
  <sub>Built with ❤️ for the RAG community</sub>
</p>
